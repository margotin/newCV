<h2>{{ title }}</h2>
<hr>

{{ form_start(form) }}
    
        <div class="form-error">
            {{ form_errors(form) }}
        </div>

        {{ form_row(form.name) }}
       

        <div class="mt-3">
            <button type="submit" class="btn btn-primary">{{ action }}</button>
            <a href="{{ path('manage_skill_category') }}" class="btn btn-secondary">Retour</a>
        </div>

{{ form_end(form) }}
